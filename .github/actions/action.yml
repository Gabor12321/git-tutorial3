name: hello world action 
on: workflow_call

jobs:
 action_job:
    runs-on: ubuntu-latest
    steps:
      # This step checks out a copy of your repository.
      - name: My first step - check out repository
        uses: actions/checkout@v4
         run: echo this is an echo from the action file 
